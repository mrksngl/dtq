
SUFFIXES = .c .h .y .l

AM_YFLAGS = -d

# binary programs
bin_PROGRAMS=dtq
# sources of that program
dtq_SOURCES=dtq.c dtq.h dtq-parse.h dtq-parse.c dtq-bison.y dtq-flex.l

BUILT_SOURCES = dtq-bison.h